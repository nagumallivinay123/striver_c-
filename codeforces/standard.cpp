#include <bits/stdc++.h> 
using namespace std;
#define int long long int
 const int mod = 1e9 + 7;
int dp[2][1000001];
int ans[1000001];
int Consistent(int N)
{
 
    for (int i = 2; i <= N; i++)
    {

        
    }
 
}
int32_t main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    int N;
    cin >> N;
    int out_;
    out_ = Consistent(N);
    cout << out_;
    return 0;
}